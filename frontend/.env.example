# Backend API base URL (use ngrok URL when demoing via Vercel)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket URL (use wss:// with ngrok when demoing via Vercel)
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws
