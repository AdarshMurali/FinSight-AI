# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

# design-comparison scratch routes — local-only, never deployed.
# Bloomberg won the comparison and is promoted to "/" (see AppShell.tsx);
# these snapshots stay on disk for reference but are excluded from git/prod.
/app/home-hdfc/
/app/home-hdfc-v1/
/app/home-bloomberg/
/components/HdfcSidebar.tsx
/components/HdfcSidebarV1.tsx
